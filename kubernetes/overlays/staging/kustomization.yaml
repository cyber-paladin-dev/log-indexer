apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: log-indexer-staging

bases:
  - ../../base

patchesStrategicMerge:
  - replica-patch.yaml

commonLabels:
  environment: staging
