apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: log-indexer-dev

bases:
  - ../../base

patchesStrategicMerge:
  - replica-patch.yaml
  - configmap-patch.yaml

commonLabels:
  environment: dev
